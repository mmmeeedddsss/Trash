#include<stdio.h>
#include<stdlib.h>
/*int merge(int *s,int p,int q,int r)
{
     int *a;
     a=malloc(sizeof(int));
     int *b;
     b=malloc(sizeof(int));    
}
int mergesort(int *s,int p,int r)
{
     if(q<r)
     {
          q=(p+r)/2;
          mergesort(s,p,q);
          mergesort(s,q+1;r);
          merge(s,p,q,r);    
     }
}
*/
int main()
{
     int a;
     scanf("asd%d",&a);
     printf("%d",a);    
}
